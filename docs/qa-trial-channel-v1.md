# 美团试新频道 V1 QA 报告

- 日期：2026-07-25
- 范围：美团入口、试新频道、AI 需求理解、三家推荐、三家店铺详情、结算、订单状态、餐后验证、证据成长、商家经营台
- 视口：375 × 812 用户端；1440 × 900 商家端
- 技术形态：React SPA + Hono API + SQLite
- 初始健康度：93/100
- 最终健康度：97/100

## 验证结果

| 检查项 | 结果 |
|---|---|
| 静态检查 | 通过；保留 2 条 Fast Refresh 结构提示 |
| 单元与集成测试 | 31 个测试文件、77 项测试全部通过 |
| 生产构建 | 通过 |
| 浏览器端到端测试 | 4 条完整路径全部通过 |
| 移动端横向溢出 | 未发现 |
| 桌面端横向溢出 | 未发现 |
| 浏览器控制台错误 | 未发现 |
| 三店独立详情与套餐 | 全部可进入、可选套餐、可进入结算 |
| 用户端与商家端数据账 | 9/9、8/9、7/9、9/10 一致 |
| AI 无密钥兜底 | 使用基础理解模式，流程不中断 |

## ISSUE-001：商家端证据指标可读性不足

- 严重度：Medium
- 类别：视觉 / 信息层级
- 复现：在 1440 × 900 打开 `/#/merchant/store-beef-01`，三项证据数字和标签挤在同一行，绿色数字对比度较低；成长进度条使用浏览器默认蓝色。
- 修复：将三项证据改为独立指标卡，数字、口径分层显示；数字与进度条统一使用美团黄；成长区改为明确的两列布局。
- 状态：Verified
- Commit：`3505bec`
- 文件：`src/merchant/merchant.css`
- 复验：桌面截图检查通过，无横向溢出、无控制台错误。

## 数据与测试稳定性

端到端测试使用独立 API 端口、独立前端端口和专用 SQLite 数据库。每次测试前清理专用数据库并自动迁移、播种，避免旧种子数据导致 10/40/22 与当前 8/34/19 口径混用。每条会改写证据的测试结束后重置体验空间，避免测试之间相互污染。

## 暂不阻塞交付的提示

`oxlint` 仍报告 `DemoSessionProvider.tsx` 的两条 Fast Refresh 文件结构提示。这不影响构建、运行或业务行为，当前版本不为消除开发期提示而拆分上下文文件。

## 交付判断

V1 已具备比赛初版作品的完整度：它不是固定脚本页面，而是可重复操作、后端真实写入、用户端与商家端共享数据账的本地全栈产品。当前可以进入内容细化、真实数据替换与公开部署准备阶段。

PR 摘要：QA 发现 1 个中等视觉问题并完成修复，健康度从 93 提升至 97。
